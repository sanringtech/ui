---
"@sanring/cli": minor
---

New `sanring search <query>` command. Searches component names and descriptions (name matches ranked first), highlights the matched substring, and shows a ✔ badge next to already-installed components when run inside an Angular project.
