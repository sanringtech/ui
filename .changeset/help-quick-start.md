---
"@sanring/cli": patch
---

`sanring --help` now prints a "Quick start" block with the actual init/add command sequence and a note that no npm install is needed, instead of leaving first-time users to find that in the README.
