---
"@sanring/cli": patch
---

Update component peer dependency handling so `sanring add` also prompts to install peer dependencies when the consumer project has a different version spec, keeping copied components aligned with the registry metadata.
