---
"@sanring/cli": patch
---

Refresh the README (shown on the npm package page): the `diff`/`update` command docs described the old "just prints every diff" behavior instead of the current safe-to-update vs needs-review distinction, and there was no summary of the CLI's current standout features up top.
