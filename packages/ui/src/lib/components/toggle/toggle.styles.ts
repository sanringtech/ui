import type { ToggleSize } from './toggle.types';

export const COMPACT_CONTROL_SIZE_CLASSES: Record<ToggleSize, string> = {
  sm: 'h-8 px-2.5 text-sm',
  md: 'h-10 px-3 text-sm',
  lg: 'h-11 px-4 text-base',
};
