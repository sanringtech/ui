import type { DatePickerSize } from './date-picker.type';

export const GRANULARITY_CELL_SIZE_CLASSES: Record<DatePickerSize, string> = {
  sm: 'h-9 px-2 text-xs',
  md: 'h-10 px-3 text-sm',
  lg: 'h-12 px-4 text-base',
};
